[Unit]
Description=Redirect web ports
After=network.target

[Service]
Type=oneshot
RemainAfterExit=true
ExecStart=/sbin/redirect-web-ports enable
ExecStop=-/sbin/redirect-web-ports disable

[Install]
WantedBy=multi-user.target
